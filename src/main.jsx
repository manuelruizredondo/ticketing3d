import React from 'react';
import { createRoot } from 'react-dom/client';
import CinemaConfigurator from './CinemaConfigurator.jsx';

createRoot(document.getElementById('root')).render(<CinemaConfigurator />);
